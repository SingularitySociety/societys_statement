---
title: "AIハーネス入門 — AIに安全に良い仕事をさせる環境づくり"
parent: "開発の心得"
nav_order: 17
has_children: true
---

# AIハーネス入門 — AIに安全に良い仕事をさせる環境づくり

AI(Claude Code など)と開発するとき、成果の質を決めるのは「AIへの頼み方」よりも、**AIが働く環境をどれだけ整えたか**です。この環境一式を**ハーネス(harness)**と呼びます。

このシリーズは、実際に運用されているプロジェクト([MulmoClaude](https://github.com/receptron/mulmoclaude) など)の設定を題材に、ハーネスの主要な部品を具体的な設定サンプルつきで解説します。

- [第1章 CLAUDE.md の書き方 — AIへの「就業規則」を網羅的に](./01-claude-md.md)
- [第2章 AI向けガチガチ lint — Vue/React + Express の ESLint 設定](./02-lint.md)
- [第3章 subagent と cross review — AIの仕事を別のAIがチェックする](./03-cross-review.md)

---

## ハーネスってなに？

ハーネス(harness)は、もともと**馬具** — 馬に着ける手綱や引き具のことです。馬の力を殺さずに、進む方向と速度を人が制御するための道具です。

AI開発の文脈では、**AIが安全に・良い仕事をできるように、人間が整えておく「ルールと道具の一式」**を指します。AIの能力はそのままに、暴走や脱線だけを防ぐ、という発想です。

### なぜ必要？ — 「口頭の注意」は毎回忘れられる

AIとの会話で「テスト書いてね」「any 使わないでね」と注意しても、その効果は**その会話の中だけ**です。次のセッションの AI は何も覚えていません。人間の新人なら一度言えば覚えますが、AI は毎朝記憶を失った状態で出社してくる新人のようなものです。

だから、注意を**環境に埋め込みます**。

| 口頭の注意(毎回忘れられる) | ハーネス(毎回自動で効く) |
| --- | --- |
| 「any 使わないでね」 | ESLint が `any` をエラーにする |
| 「コミット前にテスト回してね」 | commit しようとすると自動でテストが走る |
| 「このプロジェクトでは yarn を使ってね」 | CLAUDE.md に書いておく(毎セッション自動で読まれる) |
| 「勝手にファイル消さないでね」 | 権限設定で削除コマンドを許可しない |
| 「レビューしてからマージしてね」 | CI が PR ごとに別のAIレビューを自動で走らせる |

**「気をつけてね」と言う代わりに、気をつけなくても事故が起きない環境を作る** — これがハーネスの中心思想です。

### AI時代にハーネスの価値が跳ね上がった理由

ハーネスの部品(lint、テスト、CI)自体は昔からあります。AI時代に重要度が跳ね上がったのには理由があります。

1. **AIは書く量が桁違いに多い** — 人間のレビューが追いつかない。機械のチェックで先に止めるしかない
2. **AIは指摘されれば文句を言わず直す** — lint エラーは人間には小言だが、AIには「自動修正ループの燃料」。厳しくすればするほど品質が上がる
3. **AIは記憶を持たない** — 環境に埋め込まれたルールだけが、セッションをまたいで一貫性を保つ

つまり、**人間チームでは「厳しすぎて運用できない」ルールが、AIチームでは低コストで運用できる**。だから遠慮なくガチガチにしてよい(第2章で実例を見ます)。

---

## ハーネスの部品一覧

| 部品 | 何をするか | どこで設定するか | 詳しくは |
| --- | --- | --- | --- |
| **ルール(CLAUDE.md)** | プロジェクトの約束事を毎セッション AI に読ませる | `CLAUDE.md` / `~/.claude/CLAUDE.md` | [第1章](./01-claude-md.md) |
| **整形(formatter)** | 書き方の揺れを機械的に統一 | Prettier | [第2章](./02-lint.md) |
| **lint** | まずい書き方・バグの芽を自動で指摘 | ESLint | [第2章](./02-lint.md) |
| **型チェック** | データの形の食い違いをコンパイル時に検出 | TypeScript (`tsc --noEmit`) | [第2章](./02-lint.md) |
| **テスト** | 「壊していないか」を毎回自動で確認 | `node:test` / vitest / Playwright | [Vibe Coding](../vibe_coding.md) |
| **CI** | push のたびに上の全部を走らせる番人 | GitHub Actions | [CI/CD入門](../github_actions/README.md) |
| **フック(hooks)** | AIの特定の行動に自動チェックを割り込ませる | `.claude/settings.json` | この下 ↓ |
| **権限(permissions)** | AIができる操作の範囲を制限する | `.claude/settings.json` | この下 ↓ |
| **レビュー体制** | 別のAI・別の人の目でチェックする | subagent / CI cross review | [第3章](./03-cross-review.md) |

---

## フックと権限 — Claude Code に組み込むハーネス

CLAUDE.md(第1章)が「読んでもらう約束」なのに対し、フックと権限は**破りようがない強制力**を持ちます。設定は `.claude/settings.json` に書きます。

### 実例: コミット前に全チェックを強制するフック

MulmoClaude で実際に使われている設定です。AI が `git commit` を実行しようとすると、**その前に**ビルド・整形チェック・lint・型チェック・テストが自動で走り、失敗するとコミット自体がブロックされます。

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "yarn format --check && yarn lint && yarn typecheck && yarn build && yarn test",
            "timeout": 300,
            "statusMessage": "Pre-commit checks (format/lint/typecheck/build/test)..."
          }
        ]
      }
    ]
  }
}
```

ポイント:

- CLAUDE.md に「コミット前にテストを回すこと」と書いても、AI がうっかり忘れる可能性はゼロではありません。フックなら**物理的に忘れられない**
- 壊れたコードがコミット履歴に入らないので、「どこから壊れたか」を探す時間が消える
- 人間が手でコミットするときの pre-commit フック(husky など)の Claude Code 版だと思えばOK

### 実例: よく使う安全なコマンドだけ事前許可する

```json
{
  "permissions": {
    "allow": [
      "Bash(yarn test)",
      "Bash(yarn test *)",
      "Bash(yarn lint)",
      "Bash(yarn typecheck)"
    ]
  }
}
```

Claude Code は標準では危険になりうる操作のたびに人間へ確認を求めます。**読み取り専用・安全と分かっている操作だけを事前許可**しておくと、AIはテンポよく働けて、危険な操作(削除・push など)は引き続き人間の確認が挟まります。「全部許可」にしないのがコツです — 権限は狭いほど事故の範囲(blast radius)が小さくなります。

---

## このシリーズの読み方

- ハーネスを**これから整える**人 → 第1章(CLAUDE.md)から順に。まず約束を書き、次に機械の強制(第2章)、最後にレビュー体制(第3章)
- **すでに Claude Code を使っている**人 → 自分のプロジェクトに欠けている部品をこのページの一覧表で探して、該当章へ

## 合わせて読む

- [Vibe Coding について](../vibe_coding.md) — AIとの開発の全体的な心得。ハーネスはその土台
- [Vibe Coding 入門(非エンジニア向け)](../vibe_coding_simple.md)
- [GitHub ActionsではじめるCI/CD](../github_actions/README.md) — ハーネスをCIに載せる方法
- [Git/GitHub 入門(非エンジニア向け)](../git_basics.md) — PR・マージの基本(第3章の前提)
